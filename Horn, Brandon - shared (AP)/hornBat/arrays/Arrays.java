package arrays;

public class Arrays
{
    /*
     * Return the second largest number in values.
     * Precondition: values.length >= 2
     */
    public static int secondLargest(int[] values)
    {
        return -1;
    }
    
    /*
     * Return an array containing the x largest numbers in values.
     * The numbers in the returned array may appear in any order.
     * Precondition: x > 2 && values.length >= x
     */
    public static int[] xLargest(int[] values, int x)
    {
        return null;
    }
}
