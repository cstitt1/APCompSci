package randomArrays;

public class RandomArrays
{
	/**
	 * Generates an array containing n random integers in the range
	 * low <= value <= high. Each array element has an equal probability
	 * of containing any value in the range.
	 * 
	 * @param n the number of values in the array
	 * @param low the low end of the range for random number generation
	 * @param high the high end of the range for random number generation
	 * 
	 * (Precondition: n >= 0 low >= 0 && && low < high)
	 * 
	 * @return an array containing n random integers in the range low <= value <= high
	 */
	public static int[] randomNumbers(int n, int low, int high)
	{
		return null; // TODO: replace
	}
	
	/**
	 * Generates an array containing n distinct random integers in the range
	 * low <= value <= high. Each value in the range has an equal probability
	 * of being included in the returned array.
	 * 
	 * @param n the number of values in the array
	 * @param low the low end of the range for random number generation
	 * @param high the high end of the range for random number generation
	 * 
	 * (Precondition: n >= 0 && low >= 0 && low < high && high - low + 1 >= n)
	 * 
	 * @return an array containing n distinct random integers in the range low <= value <= high
	 */
	public static int[] distinctRandomNumbers(int n, int low, int high)
	{
		return null; // TODO: replace
	}
}
